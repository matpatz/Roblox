# Pastefy API token — used by the notepad page (via api/v1/pastefy.js)
# Copy this file to .env and fill in your token from https://pastefy.app/settings
# (or set the same var in Vercel: Project → Settings → Environment Variables)
PASTEFY_API_KEY=
