its not like the typical knit, its my own thing

instead of loadstirng maxxing you will call:

```lua
const Options = {
    "main",
    "core",
    "utils"
}
Knit.require(shared.script, Options[1])
```